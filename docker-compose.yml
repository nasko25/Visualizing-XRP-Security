version: "3.9"
services:
  server:
    build: server/
    ports:
      - "8080:8080"
    volumes:
      - "server:/app"
    image: "server"

  client:
    build: client/
    ports:
      - "3000:3000"
    volumes:
      - "client:/client"
    image: "client"

volumes:
  server:
  client:
